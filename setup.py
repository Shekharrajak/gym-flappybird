from setuptools import setup

setup(name='gym_flappybird', # what you will use to import your environment
      version='0.0.1',
      install_requires=['gym','pygame']  # And any other dependencies foo needs
)
